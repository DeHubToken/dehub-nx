query teamMembers($isPreview: Boolean = false) {
  teamMemberCollection(preview: $isPreview) {
    items {
      sys {
        publishedAt
      }
      name
      title
      avatar {
        url
      }
      twitter
      linkedin
      instagram
      github
    }
  }
}
