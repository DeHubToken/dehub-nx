export enum LoadingStatus {
  LOADING = 0,
  SYNCHRONIZING = 1,
  PAUSED = 2,
  COMPLETE = 3,
}

export enum FetchStatus {
  NOT_FETCHED = 'not-fetched',
  SUCCESS = 'success',
  FAILED = 'failed',
}
